package com.ccamacho.kotlinprogrammingstudy.collections

/*
Main Class

        val numberList = listOf<Int>( 3, 7, 78, 2, 1 )
        val predicate = { item: Int -> item > 10 }

//        val checkAll = numberList.all { it > 10 }
        val checkAll = numberList.all(predicate)
        println("Are all elements greater then 10? $checkAll")
        //Are all elements greater then 10? false

//        val checkAny = numberList.any { it > 10 }
        val checkAny = numberList.any(predicate)
        println("Does any of these elements satisfay the Predicate? $checkAny")
        //Does any of these elements satisfay the Predicate? true

//        val count = numberList.count { it > 10 }
        val count = numberList.count(predicate)
        println("Number of elements that satisfy the Predicate: $count")
        //Number of elements that satisfy the Predicate: 1

//        val findNumber = numberList.find { it > 10 }
        val findNumber = numberList.find(predicate)
        println("First number with match the Predicate: $findNumber")
        //First number with match the Predicate: 78
 */